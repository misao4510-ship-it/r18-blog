---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

export async function getStaticPaths() {
  const allPosts = await getCollection('posts', ({ data }) => !data.draft);

  const voiceMap = new Map<string, typeof allPosts>();
  for (const post of allPosts) {
    for (const va of (post.data.voice_actresses ?? [])) {
      if (!voiceMap.has(va)) voiceMap.set(va, []);
      voiceMap.get(va)!.push(post);
    }
  }

  return [...voiceMap.entries()].map(([name, posts]) => ({
    params: { slug: name },
    props: { name, posts: posts.sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()) },
  }));
}

const { name, posts } = Astro.props;
---
<BaseLayout
  title={`${name} のレビュー一覧`}
  description={`${name}出演のFANZA同人・ボイス作品レビューを雪音がまとめてご紹介します。`}
  yukineVariant="works"
>
  <div class="container" style="padding:2rem 1rem;">
    <div style="margin-bottom:1rem;">
      <a href="/voices/" style="color:var(--color-text-muted);font-size:0.9rem;">← 声優一覧</a>
    </div>
    <h1 style="font-size:1.5rem;font-weight:900;margin-bottom:0.4rem;color:#e040fb;">🎤 {name}</h1>
    <p style="color:var(--color-text-muted);margin-bottom:2rem;font-size:0.9rem;">{posts.length}本のレビューを掲載中</p>

    <div class="article-grid">
      {posts.map(post => (
        <article class="article-card">
          <a href={`/posts/${post.id}/`}>
            {post.data.heroImage && (
              <img src={post.data.heroImage} alt={post.data.title} loading="lazy" />
            )}
            <div class="article-card__body">
              <div class="article-card__title">{post.data.title}</div>
              <div class="article-card__desc">{post.data.description}</div>
              <div class="article-card__date">
                {new Date(post.data.pubDate).toLocaleDateString('ja-JP', {
                  year: 'numeric', month: 'long', day: 'numeric'
                })}
              </div>
              {post.data.circle && (
                <div style="margin-top:0.4rem;font-size:0.78rem;color:var(--color-text-muted);">
                  サークル: <a href={`/circles/${encodeURIComponent(post.data.circle)}/`} style="color:var(--color-accent);">{post.data.circle}</a>
                </div>
              )}
              {post.data.tags && post.data.tags.length > 0 && (
                <div class="article-tags" style="margin-top:0.5rem;">
                  {post.data.tags.map(tag => (
                    <span class="article-tag">{tag}</span>
                  ))}
                </div>
              )}
            </div>
          </a>
        </article>
      ))}
    </div>

    <div style="margin-top:2rem;">
      <a href="/" style="color:var(--color-accent);">← 記事一覧に戻る</a>
    </div>
  </div>
</BaseLayout>
